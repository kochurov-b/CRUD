scalar Email

type User {
  id: ID!
  email: Email!
  name: String!
}

type UsersInfo {
  users: [User]
  totalCount: Int!
}

input CreateUserInput {
  email: Email!
  name: String!
}

input UpdateUserInput {
  email: Email
  name: String
}
